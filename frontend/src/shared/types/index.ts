export type ChatListUserItem = { id: string; name: string; hasNewMessage: boolean };
export type ChatListRoomItem = { id: string; name: string; hasNewMessage: boolean };

export type ChatsListItem = { id: string; name: string; hasNewMessage: boolean };

export type User = { id: string; username: string; role: string };
